 1)
 The difference is an has-a relationship is inheritance. The classes which inherit are known as sub classes an example would be car which is a sub class inherited from vehicle
 while is-a relationship is composition.
 
 2)
 It would be available to both due to 
 
 3)
 An abstract method is a forces its immediate subclass to implement the function of all abstract methods. 
 Where as overriding is optional and is not always a necessity for the subclasses. 